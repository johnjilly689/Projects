<!DOCTYPE html>
<html>
  <head>
    <title>Taco Recipes</title>
    <link rel="stylesheet" type="text/css" href="main.css" />
  </head>

  <body>
    <h1>Taco Town 🌮</h1>

    <form action="/recipe" method="POST" class="buttons">
      <button type="submit" value="chicken" name="choice">🍗</button>
      <button type="submit" value="beef" name="choice">🥩</button>
      <button type="submit" value="fish" name="choice">🐟</button>
    </form>
    <div id="recipeContainer" <% if(!recipe) { %>class="hidden"<% } %>>
      <% if(!recipe) { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
      <% } else { %>
      <h2><%= recipe.name %></h2>
      <h3>Ingredients:</h3>
      <ul id="ingredientsList">
        <li><strong>Protein:</strong> <%= recipe.ingredients.protein.name %> - <%= recipe.ingredients.protein.preparation %></li>
        <li><strong>Salsa:</strong> <%= recipe.ingredients.salsa.name %> (Spiciness: <%= recipe.ingredients.salsa.spiciness %>)</li>
        <% recipe.ingredients.toppings.forEach(function(topping) { %>
        <li><strong><%= topping.name %>:</strong> <%= topping.quantity %> - <%= topping.ingredients.join(", ") %></li>
        <% }); %>
      </ul>
      <p><strong>Price:</strong> $<%= recipe.price %></p>
      <% } %>
    </div>

    <h1>inserted ejs value is: <%=title_2 %></h1>
  </body>
</html>
